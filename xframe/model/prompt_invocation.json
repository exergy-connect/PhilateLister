{
  "name": "PhilateLister stamp catalog",
  "version": "26.05.08.1",
  "author": "Exergy ∞ LLC",
  "description": "Entity shard: one appraisal API call (provider + model) per prompt; parallel invocations per prompt_id.",
  "source": "PhilateLister",
  "entities": [
    {
      "name": "prompt_invocation",
      "brief": "Parallel model/provider target for a prompt template.",
      "description": "Rows live in xframe/data/prompt_invocations.json. All enabled invocations for a prompt_id run in parallel. Set enabled false to skip a row without deleting it. Exactly one enabled row per prompt should have primary_for_listing true; that output is listings/<stem>.{txt|json}. Others are listings/<stem>__<invocation_id>.{txt|json}. If a prompt_id has no enabled rows, appraisal falls back to a single Gemini call using prompt.preferred_model.",
      "primary_key": "invocation_id",
      "fields": [
        {
          "name": "invocation_id",
          "description": "Unique id (used in sidecar filenames); e.g. stamp_listing_openrouter.",
          "type": { "primitive": "string" },
          "required": true
        },
        {
          "name": "prompt_id",
          "description": "Prompt template this invocation belongs to.",
          "type": {
            "primitive": "string",
            "foreignKeys": [{ "entity": "prompt" }]
          },
          "required": true
        },
        {
          "name": "provider_id",
          "description": "Provider row (provider_id selects the appraisal client; see provider entity).",
          "type": {
            "primitive": "string",
            "foreignKeys": [{ "entity": "provider" }]
          },
          "required": true
        },
        {
          "name": "model",
          "description": "Model id for this provider (Gemini model id or OpenRouter model slug, e.g. openai/gpt-4o-mini).",
          "type": { "primitive": "string" },
          "required": true
        },
        {
          "name": "sort_order",
          "description": "Stable ordering for logs and merge; lower runs first in listings.",
          "type": { "primitive": "integer" },
          "required": true
        },
        {
          "name": "primary_for_listing",
          "description": "If true, this invocation's output is the main listings/<stem> file the uploader polls.",
          "type": { "primitive": "boolean" },
          "required": true
        },
        {
          "name": "enabled",
          "description": "If false, appraisal skips this invocation. When omitted, treated as true.",
          "type": { "primitive": "boolean" },
          "required": false
        }
      ]
    }
  ]
}
